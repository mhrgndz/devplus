export enum VehicleNoteType {
    Admin = 1,
    Customer = 2,
}

export enum VehicleOperationStatus {
    Active = 1,
    Complete = 2,
    InCompleteProcess = 3,
    InPaymentProcess = 4,
    WaitingForPaymentResult = 5,
}